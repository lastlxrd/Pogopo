# STEP11.1.1 — Celeste clamp compile fix

## Before

Celeste failed to compile because `std::clamp()` received mixed `float` and `double` argument types.

## Changes

The clamp value and limits were explicitly converted to `lua_Number`.

## Result

The project compiled successfully, with no runtime behavior changes.