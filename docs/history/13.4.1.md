# STEP13.4.1

- Cached small `main.pdz` archives in PSRAM for faster module imports.
- Deferred short sound decoding until first playback.
- Added `game`, `spr`, `timer`, `ft`, and `other` frame profiling.
- Cleaned obsolete documentation redirects and merge notes.
