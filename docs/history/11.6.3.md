# STEP11.6.3 — Playdate dither patterns and complete frame-timer overloads

## Before

STEP11.6.2 fixed Noble focus rendering and Duel Of Shadows font/audio issues, but Maze still stopped on the missing dither API and Duel's charged attack exposed an incomplete `frameTimer.new()` implementation.

## Changes

Real `setDitherPattern()` support with SDK patterns was added, while frame timers now correctly handle both callback and value/easing overloads together with delay, callbacks, repeats, reverses and lifecycle controls. :contentReference[oaicite:0]{index=0}

## Result

Maze can progress past its Noble dither setup and Duel Of Shadows completes the charged B-hold regression without errors while the existing PDX regression suite remains stable.