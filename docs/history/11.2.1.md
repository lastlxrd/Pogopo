# STEP11.2.1 — SD PDX launcher stack and hot-mount fix

## Before

Opening the SD Playdate browser with a valid PDX package caused a stack overflow and restarted the menu.

## Changes

The temporary PDZ index was moved to heap/PSRAM, package scanning was simplified and SD hot-mount retry support was added. :contentReference[oaicite:0]{index=0}

## Result

After building and flashing, the browser opened and detected Celeste, but launching it became stuck indefinitely on `LOADING GAME: CELESTE CLASSIC` with the same log sequence repeating.